SELECT 
    S.STORE_NAME,
    SUM(SA.TOTAL_AMOUNT * SA.QUANTITY_SOLD) AS TotalRevenue
FROM
    Stores S
        JOIN
    Sales SA ON S.STORE_ID = SA.STORE_ID
GROUP BY S.STORE_NAME
ORDER BY TotalRevenue DESC
LIMIT 5;
